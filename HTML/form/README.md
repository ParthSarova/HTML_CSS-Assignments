The Form.html file functions as a practical CSS logic lab disguised as a student registration form. It includes standard web inputs like text fields, radio buttons, and checkboxes. The primary goal of this page is to visually demonstrate the CSS specificity hierarchy, using color-coded borders to clearly show how element tags, class selectors, ID selectors, and the !important rule override one another.
